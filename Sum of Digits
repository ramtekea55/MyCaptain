#include<stdio.h>
int main()
{
 int num;
 printf("Enter the number: ");
 scanf("%d",&num);
 printf("Sum of digits: %d", sum(num));
 return 0;
}
int sum(int n)
{
 int x;
 if (n==0)
  {
   return 0;
  }
 else 
  {
   return( n%10 + sum(n/10) );
  }
return 0;
}
  
